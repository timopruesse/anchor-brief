# Optional public HTTPS endpoint for story votes (Apps Script web app — see docs/votes.md).
# POST JSON body: { "briefId": string, "itemId": string, "vote": 1 | -1, "ts": number }
# Sink appends to data/votes.jsonl via repository_dispatch — leave unset for local-only (soft-fail).
PUBLIC_VOTE_URL=
